@h3 Linux Shell Conditions & Loops

@h3 if-elif-else

@h4 if-elif-else syntax
@start-textarea
if test-commands; then
  consequent-commands;
[elif more-test-commands; then
  more-consequents;]
[else alternate-consequents;]
fi
@end-textarea

@h3 case
@h4 case syntax
@start-textarea
case word in
    [ [(] pattern [| pattern]…) command-list ;;]…
esac
@end-textarea

@h3 Loops
@h4 To Be Done